import path from "path"; import { defineConfig } from "vite"; import react from "@vitejs/plugin-react"; export default defineConfig(({ command }) => { return { base: command === "build" ? "/BK_SPACE/" : "/", plugins: [react()], resolve: { alias: { "@": path.resolve(__dirname, "./") } }, define: { "process.env": {}, "global": {} }, build: { sourcemap: false }, server: { port: 3000, host: "0.0.0.0" } }; });